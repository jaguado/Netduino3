using System;
using Microsoft.SPOT;

namespace JAM.Netduino3.App
{
    public class ConfigConstants
    {
        public const string WebServerPort = "webserver.port";
        public const string NtpServer = "network.ntp.server";
        public const string TimeZone = "network.ntp.timeZone";
        public const string DdnsUpdateUrl = "network.ddns.url";
    }
}
